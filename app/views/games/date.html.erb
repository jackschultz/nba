<h1><%= @date %></h1>

<% @games.each do |game| %>
  <div class="row">
    <div class="col-md-6">
      <%= link_to "#{game.away_team.full_name} at #{game.home_team.full_name}", game %>
    </div>
  </div>
<% end %>
